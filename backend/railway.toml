[build]
builder = "dockerfile"

[deploy]
startCommand = "python -c 'from main import app; print(\"Import OK\")' && uvicorn main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 1
